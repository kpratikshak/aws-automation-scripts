data "aws_ami" "ubuntu"
most_resent = true

filter {
    name = "name"
    values = ["ubuntu/images/hvm-ssd/ubuntu-trusty-14.0.4-amd64-server"-*""]
}

filter {
    name= "virtualization-type"
    values=["hvm"]
}

owners = [""] id 
}

resources "aws_instances" "web"{
    ami = "${data.aws_ami.ubuntu.id}"
    instance_type ="t2.micro"

    tags = {
        Name = "HelloWorld"
        Env = "QA"
    }
}